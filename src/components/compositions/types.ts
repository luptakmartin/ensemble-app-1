export type CompositionWithCount = {
  id: string;
  ensembleId: string;
  name: string;
  author: string;
  duration: string | null;
  createdAt: string;
  updatedAt: string;
  attachmentCount: number;
};
